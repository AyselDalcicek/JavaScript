// it imports from Elements file into elements variable.
var elements = require("../ModuleExporting/Elements.js");  

console.log(elements.username);
console.log(elements.password);
console.log(elements.logInButton);